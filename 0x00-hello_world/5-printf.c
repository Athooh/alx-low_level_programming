#include <stdio.h>
/**
  *main - the program uses printf function to output a string
  *Return: This returns 0
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
